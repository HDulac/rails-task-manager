<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p>
  <%= link_to "Delete",
    task_path(@task),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
</p>
<p><%= link_to "Back to All Tasks", tasks_path %></p>
